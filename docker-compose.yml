version: '2'

services:
  master:
    build: .
    command: roscore

  talker:
    build: .
    environment:
      - "ROS_HOSTNAME=talker"
      - "ROS_MASTER_URI=http://master:11311"
    command: rosbag play -l traffic_light_bag_file/traffic_light_training.bag

  # listener:
  #   build: .
  #   environment:
  #     - "ROS_HOSTNAME=listener"
  #     - "ROS_MASTER_URI=http://master:11311"
  #   command: rosrun roscpp_tutorials listener

  resolvable:
    image: mgood/resolvable
    volumes:
      - /var/run/docker.sock:/tmp/docker.sock
      - /etc/resolv.conf:/tmp/resolv.conf
